# PinNote · 使用说明

## 运行方式

方式一（推荐）：运行打包好的安装包

```
dist\PinNote Setup 1.4.0.exe
```

按向导安装（可自选安装目录），从桌面或开始菜单的 **PinNote** 快捷方式启动。

方式二：源码运行（需 Node.js）

```
npm install
npm start
```

## 数据位置

- 项目数据（设计稿、标注、建议、参考图）：`%APPDATA%\PinNote\store\`
- 所有导入的图片都会复制到该目录，删除原图不影响已有项目；卸载软件不会自动删除项目数据。
- 程序缓存与自动化测试的隔离数据在项目文件夹的 `.runtime-cache\`，与正式数据完全无关。

## 功能速览

| 功能 | 说明 |
|---|---|
| 界面布局 | 无边框标题栏（顶栏可拖动窗口，右侧为最小化/最大化/关闭）；左侧项目栏可收起（«收起，»展开） |
| 项目/版本管理 | 左侧栏新建项目（自动建 V1）、添加版本（V2/V3 递增），二者均可选择「上传设计稿」或「创建空白项目/版本」 |
| 空白项目 | 直接创建空白项目后，在设计稿投放区通过按钮上传、拖拽或 Ctrl+V 粘贴补图 |
| 粘贴图片 | 任意位置 Ctrl+V 粘贴剪贴板图片：空画布直接进设计稿，画布已有图时弹窗选择「设计稿/参考图」；支持一次粘贴多张 |
| 归档 | 右键项目菜单或顶栏「归档/取消归档」；已归档项目只读，可查看、可导出 PDF |
| 右键菜单 | 右键项目：重命名/归档/删除；右键版本：重命名/删除（最后一个版本不可删；悬停 × 快捷删除仍可用） |
| 设计稿导入 | 顶栏「添加设计稿」按钮、双击画布、拖拽图片到画布区、Ctrl+V 粘贴，多张长图纵向无缝拼接 |
| 标注 | 点批注(P) 点击生成标记点；框批注(B) 拖拽生成框；鼠标(V) 选择/拖动；框右下角可拖拽调整大小 |
| 缩放 | 画板底部悬浮工具条 −/+/适应（以画板中心为锚），Ctrl+滚轮以鼠标位置为锚点缩放（指哪儿放大哪儿），快捷键 +/−/0，点击百分比数字重置 100%（范围 20%~400%） |
| 建议框 | 跟随标注自动生成，宽 260px，随内容自动增高、自动避让（16px 间距），可再编辑 |
| 自动连线 | 标注右边缘 → 建议框左侧，随标注拖动/缩放实时更新 |
| 参考图 | 右侧独立面板（贴右固定、独立滚动），拖拽图片上传（放在松手位置，粘贴的落在最后一张下方），自动命名 图①图②…，可拖动调位置，删除后自动重排编号 |
| 自动保存 | 所有变更 0.6s 防抖自动保存；顶栏显示 保存中.../已自动保存/保存失败 |
| 导出 PDF | 一键导出长页单页 PDF（2x 超采样），含画布+标注+连线+建议框+参考图；文件名 `项目_版本_设计稿修改建议_日期.pdf` |
| 更新日志 | 顶栏「…」菜单随时查看；新版本首次启动自动弹出 |
| 快捷键 | Q 鼠标 / W 点批注 / E 框批注 / Ctrl+V 粘贴 / Ctrl+S 保存 / Delete 删除选中标注 / Esc 关闭弹窗·退出工具 |

## 已知限制

- PDF 单页高度受 Chromium 限制（约 5080mm），极端超长图（如 16000px）会自动等比缩放至上限内，内容完整不裁切。
- 当前仅支持 Windows。

---

开发与自动化测试相关请阅读《交接说明-面向新开发Agent.md》。
